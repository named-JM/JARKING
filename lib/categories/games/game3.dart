import 'package:flutter/material.dart';

class JacknPoi extends StatelessWidget {
  const JacknPoi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.grey,
        title: Text('JACKENPOIPAGE'),
      ),
      body: Text('this is jackenpo gaimeeee!'),
    );
  }
}
