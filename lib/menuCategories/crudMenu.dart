import 'package:flutter/material.dart';

class CrudMenu extends StatelessWidget {
  const CrudMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crud MEANU'),
      ),
    );
  }
}
