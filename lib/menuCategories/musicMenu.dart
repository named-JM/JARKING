import 'package:flutter/material.dart';

class MusicMenu extends StatelessWidget {
  const MusicMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MUSIC MEANU'),
      ),
    );
  }
}
